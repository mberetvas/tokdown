[project]
name = "tokdown"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Maxime Beretvas", email = "maximeberetvas@outlook.com" }
]
requires-python = ">=3.13"
dependencies = [
    "tiktoken>=0.13.0",
]

[project.scripts]
tokdown = "tokdown:main"

[build-system]
requires = ["uv_build>=0.9.25,<0.10.0"]
build-backend = "uv_build"
